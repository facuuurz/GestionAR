import PantallaVenta from "@/components/ventas/PantallaVenta";

export default function VentaPage() {
  return (
    <main className="w-full max-w-[1600px] mx-auto px-4 py-4 flex-1 flex flex-col min-h-0 h-screen">
        <PantallaVenta />
    </main>
  );
}